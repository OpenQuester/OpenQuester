index_generator:
  exclude:
    - "**.g.dart"
    - "**.freezed.dart"
    - "src/commands/**"
    - "**_stub.dart"
    - "**_web.dart"
  # Define the paths of the folders in which to generate the index files
  libraries:
    - directory_path: lib
