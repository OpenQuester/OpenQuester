/**
 * Predefined log tags for categorization and filtering.
 */
export enum LogTag {
  HTTP = "http",
  SOCKET = "socket",
  GAME = "game",
  AUTH = "auth",
  ADMIN = "admin",
  TIMER = "timer",
  ACTION = "action",
  MEDIA = "media",
  DB = "db",
  REDIS = "redis",
  CRON = "cron"
}
